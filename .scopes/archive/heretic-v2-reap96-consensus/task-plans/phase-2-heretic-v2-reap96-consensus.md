---
description: Select and validate a deterministic K96 subset plan.
---

# Tasks: heretic-v2-reap96-consensus Phase 2

## Input

- Phase 1 normalized evidence and overlap report
- K132 immutable plan and base hash-layer routing data
- K96 scope contracts and technical documentation

## Canonical Architecture / Key Constraints

- Score only members of each K132 keep set.
- Use ordinal tiers only if their nested-mask precondition passed in Phase 1.
- K96 hash routing is regenerated, never copied from K132.

## Phase 2: K96 Plan and Hash Routing

Goal: generate a reviewable K96 plan with explicit expert-selection evidence
and legal compact routing tables.

Definition of Done: all 43 layers have a deterministic 96-expert subset and
the plan validates its source digests, score boundary, and hash routing.

Tasks:

- [x] T021 [Backend] Implement consensus scoring and deterministic tie-breaking.
  - DoD: The report records per-expert evidence vectors and selected/deleted
    decisions; synthetic nested/non-nested fixtures pass.
  - Evidence: `evidence/reap96-phase2-score-report.json`, generated by
    `scripts/score_reap96_consensus.py`; its SHA256 is
    `ae090c1b70b476d7f116827d9342f8d2a7ff68ee36996912ce33a42735c47bfa`.
  - Result: K160/K216 use the accepted ordinal `2/1/0` tier; Blivion,
    REAP25, and REAP37 add one independent vote each. K178 is a 43/43 nested
    consistency audit and deliberately has zero weight. The 96/97 score
    boundary ties in 42/43 layers; 3 are resolved by a stronger evidence
    vector and 39 by K216's complete published semantic ranking. No actual
    boundary falls through to original expert ID.
- [x] T022 [Backend] Generate new Layer 0-2 `tid2eid` payloads from the final
  K96 keep sets.
  - DoD: Tables are `[129280, 6]`, compact IDs `0..95`, and contain no duplicate
    IDs per row.
  - Result: Direct survivors remain unchanged. Deleted assignments use the
    vendor-aligned normalized-cosine router-row fallback plus deterministic
    row-local greedy collision repair. The sensitivity audit found 89/108
    primary replacements and 481,044 slots differed from raw L2, while a global
    assignment improved cosine cost by only 0.0225%; raw L2 is superseded and
    no global assignment was adopted.
- [x] T023 [QA] Validate plan subset, count, source digest, score-boundary, and
  routing invariants.
  - DoD: New focused tests pass and malformed plan variants fail.
- [x] T024 [Docs] Freeze K96 plan/reports only after reviewable M2 evidence.
  - DoD: Plan SHA256, logical hash, source evidence digests, and routing
    checksums are documented without changing K132 records.

Checkpoint: `evidence/heretic-v2-reap96-consensus-plan.json` is frozen at file
SHA `578adbbd...e11` and logical SHA `e82c3649...c01cf`. Independent
verification is PASS and a second generation was byte-identical. The prior
raw-L2 plan remains as `heretic-v2-reap96-consensus-plan.raw-l2-superseded.json`
for audit history and is not a build input. Phase 2 is closed.

## Dependencies & Execution Order

Phase 2 depends on Phase 1. T022 depends on the T021 selected keep sets. T023
must pass before T024 and before Phase 3.
